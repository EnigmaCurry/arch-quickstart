# Base file for user states.

# This file defines no states and is meant to be overriden in a user's own
# dotfiles or dotfiles-private directories, in one of the following
# locations (in order of priority):

# /home/$USER/git/dotfiles-private/_salt/hosts/$HOST/states/user_states.sls
# /home/$USER/git/dotfiles-private/_salt/states/user_states.sls
# /home/$USER/git/dotfiles/_salt/hosts/$HOST/states/user_states.sls
# /home/$USER/git/dotfiles/_salt/states/user_states.sls

